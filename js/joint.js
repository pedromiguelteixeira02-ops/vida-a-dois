// Funções mantidas como stubs para não quebrar o refreshAll
// O bloco de Plano Salarial foi removido da UI

function saveJointPlan() {}   // sem UI, não faz nada

function refreshJointPlan() {
  // salaryPedro etc. já não existem no DOM — ignora silenciosamente
}
